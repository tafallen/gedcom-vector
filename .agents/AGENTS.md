# Workspace Rules

- **Regression Prevention**: Whenever a bug or regression is uncovered, implement dedicated regression unit tests in `tests/Gedcom.Vector.Tests/` to permanently prevent regressions.
